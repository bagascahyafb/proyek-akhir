services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - uploaded_files:/app/uploaded_files

volumes:
  uploaded_files:
