# Runtime Secrets

Create `groq_api_key` in this directory before starting Docker Compose. The file
must contain only the Groq API key. Secret files in this directory are ignored
by Git and excluded from the Docker build context.
